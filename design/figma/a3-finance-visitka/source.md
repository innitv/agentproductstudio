# A3 Финанс — сайт-визитка (фриланс-макеты) — source

## Статус

`indexed_with_risks` — фриланс-поставка внесена в локальный read-only индекс для последующего решения `extend|rebuild` при доработке.

## Source

- Figma URL: https://www.figma.com/design/uxJWafaNxGCMyGOamuoTWA/A3?node-id=120-2
- File key: `uxJWafaNxGCMyGOamuoTWA`
- Initial node: `120:2` (страница «A3 Финанс, веб-сайт»)
- Source type: `external_deliverable` (фриланс-подрядчик)
- Scope: `single_site_mockups` (визитка РНКО «А3 Финанс»: mobile, desktop, модалки, стейты кнопок/карточек/формы) + страница логотипов
- Цель использования: аудит качества, извлечение фактических токенов, основа для решения `extend|rebuild` перед сборкой нормальной DS
- Ingest date: 2026-07-13

## Использованные исходные данные

- Официальный Figma MCP (remote): список страниц, скриншот `120:2` (обзор 2000px + детальный 8000px, нарезан локально).
- Figma Desktop MCP (127.0.0.1:3845): `get_metadata` для `120:2` и `0:1` (полные деревья слоёв), `get_variable_defs` для `120:2`, `get_design_context` для `3873:96` (btn-cta), `3873:109` (btn-secondary), `3565:260` (hero H1).
- Remote `get_metadata` по узлам стабильно падал по таймауту — рабочий канал чтения этого файла: Figma Desktop MCP при открытом файле.

## Политика записи

Ingest не менял исходный Figma-файл. Любой canvas write требует отдельного `figma_write` approval с exact target.

## Ограничения

- Полный список страниц файла не подтверждён: remote-листинг вернул только `0:1`, при этом `120:2` существует; возможны другие страницы. Проверить в Figma Desktop при следующей сессии.
- `componentPropertyDefinitions` напрямую не читались; матрицы вариантов восстановлены из имён variant-детей и сгенерированного кода.
- Точные hex тёмно-синей секции, красного error-цвета и радиуса карточек не сняты (не критично для аудита; снять при сборке DS).
- Master-компонент `btn-resize` в дереве `120:2` не найден (видны только instances) — вероятно, на непросмотренной странице либо скрыт.
