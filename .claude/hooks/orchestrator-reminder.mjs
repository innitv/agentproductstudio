#!/usr/bin/env node
// UserPromptSubmit: инъекция краткого напоминания о manager-style дисциплине в контекст каждого хода.
// Список типов работы обязан совпадать с CLAUDE.md §0.1 — это его краткая инъекция,
// а не второй источник правды. `siteportfolio` убран 2026-07-17: портфолио вынесено
// в отдельный репозиторий, и хук предлагал классификацию, которой в правилах уже нет.
// Блок про дизайн-систему переписан 2026-07-27 под решение CLAUDE.md §6.1: раньше он
// он указывал на «working DS в design/figma/<slug>/», но рабочих DS в реестре не осталось —
// напоминание вело в пустоту. Теперь оно отражает умолчание: shadcn/ui в коде,
// токены в репозитории, витрина Storybook, приёмка машинная.
// Блок про масштаб переписан 2026-07-27 под Intake Question Gate: раньше он предлагал
// выбрать масштаб самому («не уверен → full»), а теперь оси запуска берутся у человека —
// два вопроса и утверждаемый план работ (CLAUDE.md §0.2, skill recursive-brief шаги 3.1-3.4).
process.stdout.write(
  "[Оркестрация] Главная сессия = оркестратор. Сначала классифицируй тип работы " +
  "(full product / reference-driven / quick draft / limited engineering / cleanup / external write) " +
  "и для продуктового run СПРОСИ, а не угадывай: два вопроса (макет в Figma? образец для сверки?) + показанный план работ, " +
  "из правки которого выводится масштаб (CLAUDE.md §0.2; не уверен → full). Молчаливый выбор маршрута или масштаба = process_deviation. " +
  "Продуктовые этапы делегируй субагентам через Task tool; финальный ответ pipeline собирает ТОЛЬКО оркестратор — " +
  "не выдавай сырой вывод субагента как финал. Внешние записи (Notion/Figma/deploy/git commit/push) — только после явного approval. " +
  "🔴 ДЕЛЕГАЦИЯ ОБЯЗАТЕЛЬНА, НЕ ДЕЛАЙ САМ: любую нетривиальную/многошаговую дизайн-, Figma-, DS-, визуальную или сборочную работу " +
  "НЕ выполняй линейно в главной сессии — ДЕЛЕГИРУЙ профильному субагенту (design / design-generator / qa-review) через Agent tool. " +
  "Ручная пошаговая сборка в оркестраторе = нарушение manager-style (студия агентов). " +
  "🔴 ДИЗАЙН-СИСТЕМА ПО УМОЛЧАНИЮ — shadcn/ui В КОДЕ (CLAUDE.md §6.1), не Figma и не сборка с нуля: компоненты из официального реестра " +
  "(`yarn shadcn add <component>`, список — `yarn shadcn search @shadcn`) живут в apps/frontend/src/components/shadcn/ и принадлежат проекту. " +
  "Своя DS (product_specific/bespoke) — только по явному обоснованию, записанному в design-brief.md. " +
  "Токены — DTCG в design/tokens/ (`yarn tokens:build`), НЕ в Figma. Витрина — Storybook: экран = composition story = роут приложения. " +
  "Приёмка машинная, «похоже» проверкой не считается: `yarn vr:test` (только в Docker), `yarn test-storybook`, `yarn qa:mobile`. " +
  "Figma сузилась до дивергентного черновика на 04-design и разового извлечения токенов; рабочих DS в design/figma/registry.json нет — " +
  "индексы заархивированы, не ищи там источник правды (design/figma/README.md). " +
  "Правила: CLAUDE.md; детальные gates: agent-pack/workflows/claude-operating-rules.md."
);
process.exit(0);
