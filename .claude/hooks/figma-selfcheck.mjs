#!/usr/bin/env node
// PostToolUse hook на Figma write (use_figma / generate_figma_design).
// Самопроверка по канону /figma-ds:build — это ФИНАЛЬНЫЙ/ПАКЕТНЫЙ гейт, НЕ ритуал после каждого write.
// Правило-источник живёт в плагине figma-ds (skills/build/SKILL.md, раздел «Перед отчётом — финальная самопроверка»).
// Хук фактически срабатывает на каждый write (иначе PostToolUse не умеет), поэтому здесь — КОРОТКИЙ нейтральный
// маркер: «write учтён», без флуда обязательным чек-листом на каждом промежуточном шаге. Тяжёлую проверку
// агент гоняет один раз — перед ответом пользователю / перед handoff, на всей пачке разом.

const reminder = [
  'Figma write учтён. Самопроверка /figma-ds:build — НЕ после этого write, а ОДИН раз перед ответом/handoff (финальный гейт на всю пачку).',
  'Промежуточные write в пачке скриншотить не нужно; собери изменения и проверь их вместе в конце.',
  'Перед тем как отчитаться «готово», прогнать финальный чек-лист плагина на всей пачке: чистый верхний уровень страницы (children === [board], ни один master/set/icon не валяется на канвасе), 0 сырых значений (всё bound к токенам), СКРИНШОТ витрины (не только metadata), подписи = имена токенов, и сверка с каноном дизайн-систем (skill `/figma-ds:standard` — структура И подача, НЕ живой Figma). Локальных reference-индексов в `design/figma/` больше нет: индексы заархивированы, дизайн-система по умолчанию живёт в коде (CLAUDE.md §6.1), а Figma-маршрут применяется только когда макет действительно собирается в Figma. Многошаговая DS-работа идёт через субагента (design/design-generator/qa-review), не вручную.',
].join('\n');

process.stdout.write(
  JSON.stringify({
    hookSpecificOutput: {
      hookEventName: 'PostToolUse',
      additionalContext: reminder,
    },
  }),
);
